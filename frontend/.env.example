# Backend API base URL (your Render service URL in production).
# Local dev can omit this — it falls back to http://localhost:5001
VITE_API_URL=https://your-backend.onrender.com

# Socket.IO server URL for multiplayer (usually the same as VITE_API_URL).
VITE_SOCKET_URL=https://your-backend.onrender.com
